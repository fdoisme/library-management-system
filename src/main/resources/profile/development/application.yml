server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/library-management-system
    username: postgres
    password: superUser
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        enable_lazy_load_no_trans: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  profiles:
    active: development
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

app:
  upload-directory: library-management-system-uploads
  folder-temp: ./library-management-system-temp/

logging:
  level:
    com.sinaukoding.library-management-system: TRACE

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    defaultModelsExpandDepth: -1
    docExpansion: none
    tagsSorter: alpha
    operationsSorter: alpha